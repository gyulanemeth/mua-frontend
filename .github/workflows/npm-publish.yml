name: Build & publish to NPM

on:
  push:
    branches: [master]

permissions:
  contents: read
  id-token: write

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: 'master'

      - name: Use Node.js 20.x
        uses: actions/setup-node@v4
        with:
          node-version: '20.x'
          registry-url: 'https://registry.npmjs.org'


      - name: Install dependencies
        run: npm i

      - name: Run tests
        run: npm run test:unit

      - name: Build the package
        run: npm run build

      
      - name: Debug npm auth & OIDC context
        run: |
          echo "=== npm version ==="
          npm -v

          echo "=== registry ==="
          npm config get registry

          echo "=== auth-related env vars ==="
          env | grep -iE 'npm|token|auth|oidc' || echo "none"

          echo "=== npm auth config ==="
          npm config list -l | grep -iE 'registry|authToken|_auth' || echo "none"

          echo "=== GitHub OIDC ==="
          echo "ACTIONS_ID_TOKEN_REQUEST_URL=${ACTIONS_ID_TOKEN_REQUEST_URL:-missing}"
          echo "ACTIONS_ID_TOKEN_REQUEST_TOKEN=${ACTIONS_ID_TOKEN_REQUEST_TOKEN:+present}"

      - name: Publish to npm (OIDC)
        run: |
          echo "Before cleanup:"
          echo "NPM_CONFIG_USERCONFIG=$NPM_CONFIG_USERCONFIG"
          test -f "$NPM_CONFIG_USERCONFIG" && (echo "--- .npmrc ---"; cat "$NPM_CONFIG_USERCONFIG"; echo "------------") || true

          # Remove any token-based auth and temp npmrc created earlier
          unset NODE_AUTH_TOKEN
          unset NPM_TOKEN
          rm -f "$NPM_CONFIG_USERCONFIG" || true
          export NPM_CONFIG_USERCONFIG="$HOME/.npmrc"

          echo "After cleanup:"
          echo "NPM_CONFIG_USERCONFIG=$NPM_CONFIG_USERCONFIG"
          npm config list -l | grep -iE 'registry|authToken|_auth' || true

          npm publish


